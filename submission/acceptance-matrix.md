# Acceptance matrix

This matrix maps every item in the project definition of done to current, inspectable evidence. A green local command is not treated as proof for a requirement it does not exercise.

| # | Requirement | Status | Authoritative evidence |
|---:|---|---|---|
| 1 | A new visitor understands the objective without documentation. | Verified | The production shell renders the objective, lesson premise, ordered investigation steps, and primary cast action; `tests/e2e.test.mjs` checks the server-rendered copy and `tests/browser-journey.test.mjs` checks the visible journey. |
| 2 | The Moonflower scenario completes from failure through a constraint-preserving recast. | Verified | The production-browser test performs the complete journey and asserts failure, sacred ducks, Stable v3, and blooming Moonflower. |
| 3 | The spell is a typed directed graph rather than a cosmetic canvas. | Verified | `src/domain/spell.ts` defines node and edge vocabularies, endpoint compatibility, graph validation, versioned edits, and deterministic serialization; domain tests exercise accepted and rejected connections. |
| 4 | Casting is deterministic for a graph and seed. | Verified | `src/simulator/cast.ts` is pure; the spell-engine suite compares repeated cast results structurally. |
| 5 | All seven required WebMCP tools are registered with narrow schemas. | Verified | `src/tools/webmcp.ts` registers exactly seven definitions generated from `src/tools/definitions.ts`; the WebMCP suite compares the full registration contract with the shared factory output, then checks described and bounded inputs, canonical enums, shared patch/revert patterns, mutually exclusive patch/revert inputs, and `additionalProperties: false`. The dated draft audit is in `submission/webmcp-conformance.md`. |
| 6 | Read-only and mutating tools are accurately annotated. | Verified | Registration tests distinguish five reads and two writes, require the current `readOnlyHint` and `untrustedContentHint` fields, reject legacy annotation fields, and prove pre-cancelled executions never reach handlers. |
| 7 | Tool results provide structured verification evidence. | Verified | Handler tests consume graph versions, stable IDs, closed filtered subgraphs and boundary edges, filter accounting, high-level inspection status, complete ordered causal paths, typed minimal side-effect subgraphs, positive and negative rule premises, counterfactual edge-necessity checks, bounds, truncation, cycle and type evidence, cast assertions, duck counts, ranked edit and eligibility evidence, canonical proposal/simulation/application/rollback ledger receipts, explicit and validated patch preconditions, before/after summaries, verification casts, and one-use revert tokens. Detailed assertions are withheld from inspection until an explicit cast. Patch simulation additionally returns base and simulated versions plus `editorMutated: false`, while structural equality assertions prove inspection, explanation, and preview leave the live graph unchanged. |
| 8 | Agent activity is visible in the interface. | Verified | Shared handlers feed a typed result-presentation channel, activity list, graph highlighting, and the operation ledger rendered by the human card. The UI consumes the exact ledger returned to the agent rather than independently translating operations; production Chrome compares all eight visible labels with the registered tool result. Removed edges, added edges, and activated runes are simultaneously distinguished on the canvas. Human and agent preview simulations are labeled unapplied while the live version remains fixed. The production-browser test invokes all seven registered definitions directly and checks the causal failure, sacred pin, complete eight-edit preview, safe prediction, matching application and rollback receipts, applied outcome, and console-error absence. |
| 9 | Human constraints materially change the repair. | Verified | Solver tests prove an unconstrained six-edit route bypasses the ducks while the sacred constraint rejects that candidate, ranks an eight-edit Umbrella route first, and preserves all twelve ducks. Atomic patch tests independently reject a candidate that disconnects a sacred rune, plus same-version graphs missing a required edge, dormant state, or sacred lock, without mutating any rejected input. |
| 10 | No OpenAI API key is required. | Verified | The application has no model/API dependency, runtime secret, account surface, analytics, or external request. Deterministic local handlers power both WebMCP and the fallback console; the Chrome journey asserts same-origin-only traffic. |
| 11 | Keyboard, focus, reduced motion, contrast, and mobile fallback are present. | Verified | CSS/source tests enforce the 4.5:1 text-token floor plus focus and reduced motion; the production-browser test checks keyboard rune nudging, mobile order, 44px targets, objective visibility, zero horizontal overflow, and that at least 90% of the browser-agent brief plus both judge actions are visible and unobscured at 1280×720. |
| 12 | Unit, integration, production-build, and browser tests pass through `prepare.py`. | Verified locally and in CI | `python3 prepare.py --quick` passes locally; GitHub Actions performs a clean install, zero-vulnerability audit, quick acceptance, deployment-bundle checks, production response-policy assertions, and a Chrome journey covering human controls, bounded registered-tool tracing, direct registered-tool execution, and same-origin-only traffic on Node 22.13. Full local acceptance reaches only the separate live discovery gate below. |
| 13 | A deployed experience is tested in a WebMCP-capable in-app browser. | Verified live | The public Worker at <https://hexmend.hex-machina.workers.dev> exposed all seven tools through `document.modelContext`; the canonical prompt completed through those registered definitions with Stable graph v3, twelve ducks preserved, a dry room, zero console errors, and zero cross-origin requests. The timestamp, tool names, security headers, and final state are measured against the live deployment by `npm run verify:live`, which writes `tests/browser-acceptance.json` and fails rather than records if the deployed build regresses any of them. The record fingerprints the deployed document, so a stale claim is detectable instead of assumed. A stand-in host supplies `document.modelContext`, because WebMCP hosts are not broadly shipped yet. |
| 14 | Submission description, architecture, tools, limitations, screenshots, and 60 to 90 second demo exist. | Verified locally; public video pending | `submission/` contains Devpost-ready copy mapped to every judging criterion, architecture, tool inventory, limitations, three validated 1280×720 captures, the concise 75-second storyboard, and a 154.5-second H.264/AAC real browser-agent screencast with narration and a reproducible renderer. Internal links and required sections are tested. Uploading the MP4 to public YouTube remains the sole external media gate. |
| 15 | Shared production handlers power a deterministic Agent Gym episode with visible scoring and exportable trajectories. | Verified | `src/eval/agent-gym.ts` exposes rollout-safe reset/step/snapshot semantics and wraps the same handlers passed to WebMCP. Every snapshot commits to the immutable public reset task, initial observation, and state key; every transition returns explicit reward and termination/truncation flags and records replay-complete public before/after observations plus stable state keys. `describe` and reset expose a versioned self-describing action manifest with the tool-call envelope, seven schemas, and five-read/two-write annotations; WebMCP consumes the same definition factory, while the generic manifest omits task-specific identifiers. One projection excludes simulator roles, rule IDs, and answer-key edges from inspection, replay, JSONL, and Python surfaces. Invalid calls become scored transitions; a 32-step ceiling truncates runaway episodes. The generators produce 96 deterministic opaque-ID tasks across three causal families and disjoint splits: amplified carrier, reachable feedback cycle, and missing temporal guard. Every generated task also activates one to three type-valid decoy edges outside the answer route; tests prove at least three visible topologies per family, deterministic regeneration, and unchanged failure semantics. A bounded versioned 32-bit sampler reproducibly selects across all tasks or inside one family, defaults seeded resets to train, and returns an auditable selection receipt; single and vector Python tests prove repeatability, per-slot seeds, bounds, and rejection of ambiguous reset inputs. Tests prove repeatable 23/23 canonical and held-out cross-rule trajectories using natural-language/rune-text grounding, JSONL replay export, and penalties for invalid or premature actions. Each v2 JSONL line is standalone, containing its protocols, action manifest, task prompt, initial graph/key, variant metadata, transitions, and terminal receipt; an independent bounded verifier reconstructs each environment and deterministically rejects altered reset context, metadata, actions, observations, rewards, results, terminal scores, and duplicate scenarios. A five-policy held-out suite proves reward separation at 23/18/6/4/−8, including a completed policy that overrules the human, and exposed a guessed-patch shortcut; production handlers now require current-version proposal issuance before preview or application. A group-relative exporter runs those five controls across all 64 train tasks, emits centered 14.4/9.4/−2.6/−4.6/−16.6 advantages and ten positive-margin preference pairs per task, and a bounded verifier regenerates every policy to reject changed trajectories, safety and constraint flags, rankings, advantages, or margins. `HexmendPreferenceDataset` delegates trust to that verifier, then streams bounded groups and all 640 train chosen/rejected projections without copying the corpus; exact split/family filters preserve file order and reject zero-match epochs. Cross-language tests cover 16 validation groups, all 160 pairs, an 8-group/80-pair family curriculum, bad filters, and forged artifacts. A strict JSONL subprocess bridge and dependency-free single/vector Python adapters drive isolated production handlers online; cross-process tests prove held-out and three-slot parallel rollouts with deterministic ordering and state isolation. A temporal-family regression proves the advertised rollback token is accepted by runtime validation. The live Task loader swaps the graph, session, patch-capability closure, and registration together; exhaustive integration completes 23/23 through scenario-narrowed registered tools on all 96 tasks and proves every retired tool set is removed. Production Chrome proves a held-out family swap replaces schemas and visible failure before the registered agent journey updates the scorecard. |

## Release condition

The deployment and live WebMCP gates are complete. Do not mark the project complete until the existing narrated screencast is uploaded to public YouTube, its URL is recorded in release evidence, the Devpost entry is submitted, and a final unchanged-source acceptance run passes.

The exact pre-deployment archive shape and authorized handoff sequence are documented in `submission/deployment.md`.

## Challenge eligibility gates

The official challenge additionally requires a judge-accessible live URL, a public repository with a visible open-source license, and a public YouTube demo with audio. Their authoritative current values live in `submission/release-evidence.json`; full acceptance fails until each is present. The repository, live site, and submission must remain frozen after the deadline during judging.
